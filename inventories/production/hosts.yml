---
# Production inventory. Run with: uv run ansible-playbook -i inventories/production/hosts.yml playbooks/site.yml
all:
  children:
    web:
      hosts:
        web01.prod.example.com:
    db:
      hosts:
        db01.prod.example.com:
    poller:
      hosts:
        poller01.prod.example.com:
