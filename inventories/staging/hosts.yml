---
# Staging inventory (e.g. ephemeral LXD instances from the Workshop env).
all:
  children:
    web:
      hosts:
        web01.staging.example.com:
    db:
      hosts:
        db01.staging.example.com:
